<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        .container
            .panel
                .panel__inputName Минимальное значение:
                    input(type="text").panel__minValue
                .panel__inputName Максимальное значение:
                    input(type="text").panel__maxValue
                .panel__inputName Текущее значение:
                    input(type="text").panel__curValue
                .panel__inputName Показать текущее значение:
                    input(type="checkbox" name="showValueNote" value="Показать" checked).panel__showNoteValue
                    
        script(src="https://code.jquery.com/jquery-3.5.1.min.js")
        script(src="jq.adslider.ts")
        script(src="main.ts")
